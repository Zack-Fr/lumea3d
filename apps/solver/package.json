{
  "name": "lumea-solver",
  "version": "0.0.1",
  "description": "Lumea Solver - FastAPI service with OR-Tools constraint satisfaction",
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "test": "python -m pytest",
    "lint": "ruff check .",
    "format": "black ."
  }
}